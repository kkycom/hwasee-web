# 화씨.방 협업 규칙

이 저장소는 화씨.방 전체 서비스의 기준 코드다. 작업 범위 밖의 변경을 만들지 않는다.

## 역할

- **Claude Code는 리드 개발자이자 유일한 구현 담당**이다. 조사 → 구현 → 테스트 →
  커밋 → 배포 → 실제 사이트 반영 확인까지 **끝까지** 책임진다. GitHub Actions 확인,
  로그 해석, 명령 실행은 Claude가 직접 한다.
- **Codex는 좁게 개입한다.** 디자인 방향·대안 검토, 그리고 아래 "중요한 작업"의
  위험 검토에만 읽기 전용으로 참여한다. 그 밖의 작업에서 Codex를 호출하지 않는다.
- **사용자는 명령 실행·Actions 확인·로그 해석을 맡지 않는다.** 실제 기기(모바일
  Safari 등)나 외부 서비스(카카오톡 발송 등)에서만 가능한 눈 확인 정도만 부탁한다.

## 장기 공유 메모

기존 VS Code/Claude 작업방과 Orca worktree는 대화를 직접 공유하지 않는다. 다음
메모가 검증된 과거 결정과 운영 맥락의 공용 인수인계 원본이다.

- 공용 이력 인덱스: `C:\Users\kkyco\.claude\projects\c--Users-kkyco--claude\memory\MEMORY.md`
- 화씨.방 전용 최근 이력 인덱스: `C:\Users\kkyco\.claude\projects\C--Users-kkyco--claude-hwasee-hwasee-web\memory\MEMORY.md`

- **Claude는 모든 새 작업 시작 전에** 두 인덱스를 읽고, 현재 아젠다와 직접 관련된
  `project_*.md`·`feedback_*.md`만 추가로 읽는다. 큰 메모를 습관적으로 전부 읽어
  컨텍스트를 소모하지 않는다.
- 메모는 현재 코드·실서비스보다 우선하지 않는다. 오래된 사실이나 추정은 코드,
  Git 이력, 실제 검증으로 다시 확인한다.
- 병합·배포까지 확정된 중요한 결정이나 새로 확인된 회귀는 공용 메모에 한 항목으로
  남긴다. 진행 중 가설이나 미확정 계획은 기록하지 않는다.
- 위 경로를 읽을 수 없는 환경에서는 그 사실을 보고하고, 메모가 있다고 가정하거나
  임의로 대체하지 않는다.

## 작업 등급과 절차

작업 시작 시 이 worktree의 목적과 요청 범위를 한 문장으로 확인하고, 아래 세 등급
중 하나를 고른다. 애매하면 한 단계 낮은 쪽으로 시작하되, 중간에 중요한 작업의
신호(보안·데이터·서버·SEO 구조)가 보이면 즉시 3등급으로 올린다.

### 1등급 — 빠른 작업

대상: CSS·문구·SVG·버튼·레이아웃·단일 화면 UI / 확정된 디자인을 코드로 옮기는 작업 /
기존 데이터·서버·권한 구조를 바꾸지 않는 작은 기능.

절차:

1. Claude가 바로 구현한다.
2. Claude가 자체 확인한다(관련 문법·빌드, 바뀐 화면의 기본 회귀).
3. 커밋하고 배포한다.
4. Claude가 실제 사이트 반영까지 직접 확인한다.
5. "완료 보고 방식"대로 보고한다.

별도 worktree, Codex discovery·plan, Stop Hook 대기를 쓰지 않는다.
단 `bang/index.html`을 바꾸면 `bang/sw.js`의 `CACHE` 버전 갱신과 기본 회귀 확인은 유지한다.

### 2등급 — 보통 작업

대상: 여러 화면·여러 파일을 건드림 / 기존 기능에 영향 가능성은 있음 / 그러나 보안·
데이터 구조·서버·SEO 구조 변경은 아님.

절차:

1. Claude가 구현·테스트한다.
2. Codex final 검토를 한 번 받는다(실제 diff·테스트 결과 기준).
3. `BLOCKER`가 없으면 Claude가 배포하고 라이브를 확인한다. `BLOCKER`만 고치고 재검토한다.
4. "완료 보고 방식"대로 보고한다.

기본값은 Codex 최종 검토 한 번뿐이다. discovery와 plan 검토는 구조가 크게 갈릴 때만 추가한다.

### 3등급 — 중요한 작업

대상(이때만 기존의 엄격한 절차를 전부 유지한다):

- 보안, 로그인, 세션, Firestore rules
- Cloud Functions, 포인트, 결제, 외부 API 비용
- 데이터 모델 변경·마이그레이션, 삭제 또는 되돌리기 어려운 작업
- SSG, canonical, robots, sitemap, OG, 정적 빌드·배포 구조
- 개인정보·약관
- 글로벌 릴레이·번역처럼 비용과 UGC 운영이 결합되는 큰 기능
- 원인이 불명확하고 운영 회귀 가능성이 큰 버그

절차:

1. **최신 main 기반 worktree**에서 시작한다. 원래 사용자 아젠다와 `기존 확정 맥락`
   (공유 메모에서 확인한 사실만)을 `.agent-reviews/<task-id>/agenda.md`에 적는다.
   자신의 결론·계획은 넣지 않는다.
2. **병렬 진단:** Claude는 기존 구조·원인을 조사하고, Codex는 원래 아젠다만 기준으로
   별도 반증 감사를 한다. 이 단계에서 둘 다 코드를 수정하지 않는다.
3. **대조와 계획:** Claude가 두 진단을 코드·운영 근거로 대조해 최소 변경 계획을 만든다.
   구조적 선택이나 실질적 이견이 있으면 Codex에게 계획 검토를 한 번 더 요청한다.
4. **구현:** Claude만 그 worktree에서 구현·테스트한다.
5. **최종 검토:** Codex가 실제 diff·테스트·생성 산출물을 읽기 전용으로 검토한다.
   `BLOCKER`만 Claude가 고치고 최대 두 번까지 재검토한다.
6. **마무리:** BLOCKER가 없으면 Claude가 커밋·배포·라이브 확인을 하고 보고한다.
   보안·Firebase 작업은 구현 전에 공격/실패 경로를 코드로 추적하고, 구현 뒤 그 경로가
   실제로 막혔는지 검증한다.

Codex 검토 결과는 `BLOCKER`·`WARNING`·`OPTIONAL`로 구분한다. OPTIONAL을 이유로
반복 토론하거나 범위 밖 리팩터링을 하지 않는다.

## Codex 호출 (2·3등급에서만)

- Stop Hook이 켜진 환경에서는 `scripts/start-agent-review.ps1`·`scripts/request-agent-review.ps1`로
  검토를 등록만 하면 Hook이 Codex를 `read-only`로 실행하고 결과를 저장한다.
- Hook이 없는 환경에서는 Claude가 `scripts/request-codex-review.ps1`을 직접 실행한다.
  사용자가 Codex 명령을 복사·실행하지 않는다.
- `discovery`에는 Claude의 결론·계획을 전달하지 않는다. `plan`에는 계획과 근거를,
  `final`에는 실제 diff와 테스트 결과를 전달한다.
- 리뷰 결과는 `.agent-reviews/`에만 저장하며 Git에 포함하지 않는다.
- 자동 검토가 실패하면 Claude가 원인을 짧게 보고하고 필요한 조치 하나를 제시한다.
  권한을 우회하거나 검토를 건너뛰지 않는다.

## Worktree 규칙 (3등급에서만 필수)

- 3등급 작업은 최신 main 기반의 목적별 worktree에서 한다. 1·2등급은 현재 작업공간에서
  바로 진행하되, 진행 중인 다른 작업의 변경은 보존한다.
- 하나의 worktree에는 하나의 목적만 둔다. 같은 파일을 두 worktree에서 동시에 고치지 않는다.
- 작업 시작 전 `git status`와 최근 커밋을 확인한다.

## 안전 규칙

- 비밀키, 세션 토큰, 사용자 개인정보를 출력하거나 저장소에 추가하지 않는다.
- 커밋·push·배포는 이 문서의 등급별 절차 안에서 Claude가 수행한다. Firebase 데이터
  변경, 외부 계정 설정 변경은 작업 요청에 명시된 경우에만 한다.
- 권한 우회 옵션이나 일괄 자동 승인 모드를 사용하지 않는다.

## 완료 보고 방식

모든 완료 보고는 아래 순서를 지킨다.

1. 첫 문장에 `완료됨` 또는 `배포 완료`를 명확히 쓴다.
2. 사용자가 체감하는 결과를 2~4개 bullet로 먼저 설명한다(일반인이 알아볼 표현).
3. 커밋·배포·라이브 확인 결과를 짧게 덧붙인다.
4. 정말 필요한 사용자 확인 항목만 최대 2개까지 적는다.
5. CLI 인증, 헤드리스 브라우저 제한, 대체 검증 방식, 내부 경로, 긴 오류 전문은
   사용자가 요청하지 않는 한 기본 보고에서 제외한다.
6. GitHub Actions·배포·라이브 확인은 Claude가 직접 한다. 권한 문제로 불가능할 때만
   짧게 이유와 필요한 사용자 행동 하나를 제시한다.

예시:

> 배포 완료되었습니다. 완결작 공유를 누르면 책 표지 미리보기 카드가 보이고, 카카오
> 공유에는 작품 제목·도입 문장·완결작 링크가 전송됩니다. 실제 사이트 반영과 캐시
> 갱신까지 확인했습니다. 아이폰과 카카오톡에서 한 번씩만 모양을 확인해주세요.
