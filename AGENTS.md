# 화씨.방 협업 규칙

이 저장소는 화씨.방 전체 서비스의 기준 코드다. 기능·보안·분석·성능 작업은 서로 영향을 줄 수 있으므로, 작업 범위 밖의 변경을 만들지 않는다.

## 기본 역할과 의사결정

- **Claude Code는 리드 개발자·유일한 구현 담당**이다. 기존 코드와 운영 맥락을 먼저 조사하고, 최종 구현·검증·커밋을 책임진다.
- **Codex는 독립 감사자**다. 계획 단계와 최종 diff 단계에서는 읽기 전용으로 검토하며, 구현을 먼저 하거나 Claude의 결론을 그대로 전제하지 않는다.
- 중요한 작업은 Claude의 1차 판단만으로 종료하지 않는다. Claude와 Codex가 원래 아젠다를 각각 조사한 뒤, 코드·실제 데이터·검증 결과로 결론을 정한다.
- 의견이 충돌하면 추측으로 밀어붙이지 않는다. 각 주장에 필요한 코드나 운영 근거를 확인한다. 두 번의 검토 뒤에도 중요한 결론이 갈리면 사용자에게 선택지를 보고한다.

## 검토 수준

### 독립 감사 필수

아래는 Claude의 "문제 없음" 결론도 포함해, 수정 전에 Codex의 독립 진단을 반드시 받는다.

- 보안, 인증·권한, 포인트·결제, Firebase·Cloud Functions·Firestore 규칙
- 데이터 모델, 데이터 마이그레이션, 삭제 또는 되돌리기 어려운 작업
- SEO, SSG, sitemap, canonical, redirect, 색인, AdSense·광고 배치
- 배포·CI 설정, 여러 시스템에 걸친 변경, 큰 리팩터링
- 원인이 불명확하거나 운영 회귀 가능성이 큰 버그

작은 문구·CSS·명확히 고립된 UI 수정은 Claude 단독 처리할 수 있다. 사용자가 독립 검토를 요청하면 항상 필수로 승격한다.

### 중요한 작업의 순서

1. **병렬 진단:** Claude는 기존 구조와 원인을 조사하고, Codex는 원래 사용자 아젠다만 기준으로 별도 반증 감사를 한다. 이 단계에서는 둘 다 코드를 수정하지 않는다.
2. **대조와 계획:** Claude는 두 진단을 실제 코드·운영 근거로 대조해 최소 변경 계획을 만든다. 구조적 선택이나 의견 충돌이 있으면 Codex에게 계획 검토를 한 번 더 요청한다.
3. **구현:** Claude만 지정 worktree에서 구현·테스트한다.
4. **최종 검토:** Codex가 실제 diff·테스트 결과·생성 산출물을 읽기 전용으로 검토한다. `BLOCKER`만 Claude가 수정하고 재검토한다.
5. **마무리:** BLOCKER가 없으면 Claude가 변경 파일, 검증, 남은 위험을 짧게 보고한다. push·배포는 명시적 요청이 있을 때만 한다.

Codex 검토 결과는 `BLOCKER`, `WARNING`, `OPTIONAL`로 구분한다. OPTIONAL 항목을 이유로 반복 토론하거나 범위 밖 리팩터링을 하지 않는다.

## Worktree 규칙

- `main`은 기준 작업공간이다. 중요한 작업은 Orca가 만든 목적별 worktree에서 수행하며, 진행 중인 다른 작업이 있을 때 직접 수정하지 않는다.
- 하나의 worktree에는 하나의 명확한 목적만 둔다. 예: `security-*`, `adsense-*`, `feature-*`, `debug-*`, `performance-*`.
- 같은 파일을 두 worktree에서 동시에 수정하지 않는다.
- 작업 시작 전 `git status`와 최근 커밋을 확인한다. 다른 작업의 변경은 보존한다.

## 역할 분담

- 구현 담당(Claude)은 요청 범위의 코드만 수정하고, 변경 파일·검증 결과·남은 위험을 짧게 보고한다.
- 검토 담당(Codex)은 직접 수정하지 않고, 원인 가정·숨은 의존성·권한·회귀·경합·배포 영향을 중심으로 검토한다.
- 보안·포인트·인증·Firestore 규칙 변경은 실제 호출 경로와 규칙을 함께 검증한다.

## Codex 호출 규칙

- 독립 감사는 프로젝트의 Claude Stop Hook이 자동 실행한다. Claude는 `scripts/start-agent-review.ps1`와 `scripts/request-agent-review.ps1`로 검토를 요청만 등록한다. Hook은 Codex를 `read-only` 샌드박스로 실행하고 결과를 저장한 뒤, Claude가 결과를 읽을 때까지 종료를 멈춘다.
- Hook이 작동하지 않는 환경에서만 `scripts/request-codex-review.ps1`를 수동으로 실행한다. 사용자가 Codex 명령을 복사·실행할 필요는 없다.
- `discovery` 단계에는 Claude의 결론이나 계획을 전달하지 않는다. 원래 아젠다를 기준으로 누락된 문제와 반증을 찾게 한다.
- `plan` 단계에는 Claude의 계획과 근거를 전달해 최소 안전 변경인지 검토하게 한다.
- `final` 단계에는 실제 변경 diff와 테스트 결과를 검토하게 한다.
- 리뷰 결과는 `.agent-reviews/`에만 저장하며 Git에 포함하지 않는다.
- 자동 검토가 실패하면 Hook은 한 번만 중단 사유를 Claude에게 보여 준다. 권한을 우회하거나 검토를 건너뛰지 말고, 원인을 보고한 뒤 사용자에게 필요한 조치를 요청한다.

## 안전 규칙

- 비밀키, 세션 토큰, 사용자 개인정보를 출력하거나 저장소에 추가하지 않는다.
- 배포, push, Firebase 데이터 변경, 외부 계정 설정 변경은 작업 요청에 명시된 경우에만 한다.
- 권한 우회 옵션이나 일괄 자동 승인 모드를 사용하지 않는다.

## 완료 기준

1. 필요한 문법·빌드·관련 검증을 실행한다.
2. 실제 변경 파일과 검증 결과를 보고한다.
3. 배포가 필요한 경우, 배포 전후 검증 항목을 분리해 명시한다.
